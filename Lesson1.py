""""""
r"""
id(a) - ссылка на область в памяти где хранится объект


Приоритет выполнения операций в Python
от самого низкого к самому высокому:
    =
    if – else
    or
    and
    not x
    in, not in, is, is not, <, <=, >, >=, !=, ==
    +, -
    *, /, //, %
    +x, -x
    **
    x[index], x[index:index], x(arguments...), x.attribute
    (expressions...),
    [expressions...], {key: value...}, {expressions...}
    ( )
"""